# Per-project config generated by install.sh (lives in the target repo, not here)
.security-audit.conf

# Scan artifacts (generated in target projects, never committed to the kit)
docs/security/scan-findings/
sbom.cyclonedx.json
sbom.spdx.json
.kit-version

# Local-only notes / roadmap (tracked on this machine, never published)
*.local.md

# OS / editor noise
.DS_Store
